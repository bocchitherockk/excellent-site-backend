/******************** TODO ********************/

/******************** DONE ********************/
- change DTO logic inside the service layer and not the controller
- add DTO design for roles to be consistent
- add data integrity validation layer (request level)
- add data integrity validation layer (database level)
- add custom exceptions and exception handlers
- return useful error messages
- implement jwt
- seperate RoleDto into RoleRequestDto and RoleResponseDto
- seperate UserDto into UserRequestDto and UserResponseDto
- add Profile component
- add Structure component
- add Participant component
- add Domain component
- add TrainingSession component
- add ManyToMany relationship (TrainingSession x Participant)
- add Employer component
- add Trainer component
- add ManyToMany relationship (TrainingSession x Trainer)
- change PUT '/users/{id}' to accept old_password field to verify it
- change access to endpoints to match the specifications

/******************** CANCELLED ********************/
